<h2>Search Books</h2>
<form action="/books" method="get">
    <label>Name</label>
    <input type="text" name="name" value="<%=searchOptions.name%>" autofocus>
    <button type="submit">Search</button>
</form>
<br>
<% books.forEach(book => { %>
    <div><%= book.name %></div>
<% }) %> 